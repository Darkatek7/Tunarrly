services:
  tunarrly:
    build: .
    container_name: tunarrly
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - /path/to/music:/music:ro
    restart: unless-stopped
